# Stray plan drafts generated by an out-of-session process; not the canonical
# plan (that is docs/removal_scaling_plan.md). Ignored so the repo tree stays
# clean. Remove this entry to track the file if it is ever wanted.
docs/rcs_protocol.md
